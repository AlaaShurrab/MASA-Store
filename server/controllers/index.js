const { signUpHandler } = require('./common');
const {
  getClientsHandler,
  getOrdersHandler,
} = require('./seller');

module.exports = {
  signUpHandler,
  getClientsHandler,
  getOrdersHandler,
};
