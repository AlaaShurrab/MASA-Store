const {
  getCartHandler,
} = require('./buyer');

const {
  getClientsHandler,
  getOrdersHandler,
} = require('./seller');

module.exports = {
  getCartHandler,
  getClientsHandler,
  getOrdersHandler,
};
