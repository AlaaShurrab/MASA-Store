const {
  getClientsHandler,
  getOrdersHandler,
} = require('./seller');

const {
  getFavoriteHandler,
} = require('./buyer');

module.exports = {
  getClientsHandler,
  getOrdersHandler,
  getFavoriteHandler,
};
